# Live Proof Matrix

This repository wraps newsletter control planes. A tool is not operationally
ready just because it compiles, appears in the MCP tool list, or was installed
from a hosted binary. For every new or materially changed capability, update
this matrix before coding and record the exact proof before handoff.

## Acceptance Rule

Each operational capability needs all of the following:

- a named workflow row below;
- fixture or unit coverage for the parser, route gate, redaction, and output
  contract;
- negative coverage for the most likely unsafe or misleading state;
- public docs and schema snapshot updates when the tool contract changes;
- hosted GitHub Actions validation for release candidates;
- hosted binary artifact plus checksum verification for installable binaries;
- configured-alias startup proof after install;
- live no-send proof on the target Interspire major version before operational
  use.

Tool listing, successful initialization, and checksum install are installation
proof only. They do not prove that the workflow is usable.

Do not discover this matrix one failure at a time. Before changing a capability,
identify every affected row, update the expected contract, then run the complete
focused gate for those rows before release or install.

## Interspire Draft Prep Matrix

| Workflow | Tool | Class | Required Proof | Negative Tests | Live No-Send Proof |
| --- | --- | --- | --- | --- | --- |
| Runtime posture | `interspire_status` | Read | Reports admin/XML configuration, gates, blocked operations, version target, and a capability list that matches the router tool names. | Missing config, disabled risky gates, stale alias, router/status capability drift. | Status called after install/restart on the target alias and compared with tool discovery for the same alias. |
| Admin session | `interspire_admin_session_probe` | Read | Login succeeds and allowlisted read page loads; Send start page optional. | Bad CSRF/session, Access missing, blocked route. | Probe target admin with `include_send_start=true`; no queue/stat change. |
| Settings posture | `interspire_settings_audit` | Read | Redacted SMTP, bounce, throttle, unsubscribe, and cron fields. | Secret redaction, missing tab, cron omitted when requested. | Audit target settings; no form submit. |
| Settings inventory | `interspire_settings_inventory` | Read | Fetches only allowlisted settings tabs, returns capped redacted form fields, and reports omitted secret-shaped, hidden, blank, or unchecked controls by name and reason. | Password/token/license/key/cookie fields omitted, hidden control values summarized when explicitly requested, fields plus omissions capped together, missing tab, no form submit. | Inventory target settings with `include_cron=true`; confirm no form submit and no queue/stat change. |
| List ownership | `interspire_list_owner_readback` | Read | Existing list sender/reply/bounce metadata, redacted. | Capped output, HTML enrichment failure, email redaction. | Read target lists before and after list create/update. |
| Exact contact state | `interspire_contact_state` | Read | Checks one exact email/list pair through XML `IsSubscriberOnList`, falls back to bounded XML `GetSubscribers` exact-email search when the direct probe is false, then uses exact admin-HTML corroboration when configured. Presence requires an exact active, confirmed, non-bounced, non-unsubscribed subscriber record; uncorroborated absence remains low confidence. | Wildcard or malformed email rejected before XML exact search, false direct XML probe plus exact-search positive returns presence with a method-disagreement warning, raw email redaction, no send/import/queue/stat mutation. | Read one approved seed address on the target list after install/restart; if XML/admin disagree, record the disagreement and do not use negative contact-state alone as a send-readiness blocker. |
| List creation | `interspire_list_create_preview` / `interspire_list_create_apply` | Preview/apply | Preview finds the Interspire create form and source-derived `AddList` route; apply submits browser-equivalent hidden fields, selected controls, save control when named, Referer, Origin, and current page CSRF context, creates exactly one new list, then proves requested metadata on the new edit page, using a guarded post-create metadata save if Interspire ignored Bounce Email on create. | Missing form, stale plan, missing gates, wrong action route, missing/empty form CSRF with page-level CSRF present, duplicate new ids, field persistence mismatch, webhook/multi-select state loss. | Preview and apply on a disposable target list; reread list owner metadata and confirm local bounce polling was not enabled unless explicitly requested. |
| XML scaffold candidates | deferred XML list/newsletter scaffold tools | Preview/apply | Deployment-specific compatibility evidence may identify XML list/newsletter scaffold candidates, but no XML write tool is operational until request details, permission behavior, response shape, and post-write readback are fixture-proven. | Missing XML auth, method not allowlisted, permission denial, malformed details, ambiguous success, object not created/copied, redaction leak. | Deferred. If implemented, run on a disposable target object only, then prove list/campaign readback and queue/stats unchanged. |
| Campaign inventory | `interspire_campaign_readback` | Read | Returns redacted campaign manage rows plus structured campaign ids/action flags without admin URLs or CSRF tokens. | Linked campaign-name rows, token leakage, email leakage, capped output. | Read target campaign inventory and confirm the intended source campaign id before copy/edit. |
| Campaign copy | `interspire_campaign_copy_preview` / `interspire_campaign_copy_apply` | Preview/apply | Preview finds exact Copy route; apply creates exactly one new draft id. | Wrong campaign id, stale plan, duplicate id detection, route with extra params. | Copy a known disposable/source campaign; reread new campaign row. |
| Campaign template edit | `interspire_campaign_template_update_preview` / `interspire_campaign_template_update_apply` | Preview/apply | Preview resolves body controls; apply persists name/subject/body/tracking fields and rereads them. Text-body edits against HTML-only Interspire 8 campaigns must first render Step2 with the Text+HTML format selected, then prove non-zero text bytes on body audit after apply. | Missing Step2 controls, stale plan, hidden field loss, tracking flag drift, HTML-only campaign lacking `TextContent` until Step1 format is changed. | Apply a harmless draft-only HTML change to a disposable draft; apply a harmless text-body change to an HTML-only disposable draft and audit `text_bytes > 0`; queue/stats unchanged. |
| Campaign template artifact transfer | `interspire_campaign_template_artifact_update_preview` / `interspire_campaign_template_artifact_update_apply` | Preview/apply | Preview reads only a fixed private render artifact, verifies optional bytes/SHA-256, returns no raw body, and delegates to guarded template preview; apply repeats the artifact read, saves the draft, then fails unless post-apply body audit matches the artifact hash. | Path traversal, symlink artifact, oversized artifact, bad expected digest, stale plan, raw HTML leakage, persisted hash mismatch, send boundary attempted. | Apply a private source artifact to a disposable/new draft; audit body and queue/stats unchanged. |
| Campaign body audit | `interspire_campaign_body_audit` | Read/no-send proof | Counts unsubscribe tokens, URL protocols, images, alt text, links, and hashes without raw HTML. | Missing body form, raw HTML leakage, secret leakage. | Audit target draft and record warnings. |
| Render artifact | `interspire_campaign_render_artifact` | Private artifact | Writes private preview files under approved root and returns paths/hashes only. | Path traversal, disallowed root, raw HTML in response. | Render target draft to private artifact root; open separately for visual proof if needed. |
| One-recipient campaign preview send | `interspire_campaign_test_send_preview` / `interspire_campaign_test_send_apply` | No-send proof / guarded test send | Preview reads persisted campaign subject/body hashes, binds one exact recipient plus caller-supplied preview sender, verifies queue/stat invariants, and returns a preview digest without posting. Apply requires `INTERSPIRE_GUARDED_WRITES=1`, `INTERSPIRE_SEND_CONTROLS=1`, `acknowledge_test_send=true`, one exact recipient, exact preview digest, the preview report's public subject, and exact preview HTML SHA-256, then posts only Interspire's native `Newsletters/SendPreview` route. Output stays redacted and says this path does not prove list-specific unsubscribe, merge fields, tracking, or production audience behavior. Denied apply reports must omit campaign-body proof entirely rather than returning synthetic fixture data. | Multiple recipients, missing acknowledgement with no campaign_body returned, disabled send gate, wrong expected preview digest/subject/hash, empty campaign body, extra query parameter on SendPreview route, raw email/body leakage, queue/stat row changes, response that is not an explicit successful preview send. | On the target major version, run preview against a draft and apply only to an approved seed mailbox; verify Schedule and Stats rows are unchanged before/after. Use seed-list send proof instead when unsubscribe/list metadata behavior must be proven. |
| CSV import preflight | `interspire_contact_import_preflight` | Read/local file proof | Reads only configured private roots; returns headers, aggregate counts, duplicates, invalid-looking count, and hash. | Disallowed root, `..`, non-CSV, raw row/email leakage. | Preflight a synthetic private CSV before any real audience artifact. |
| Audience hygiene checkpoint export | `interspire_audience_hygiene_export_begin` / `interspire_audience_hygiene_export_resume` / `interspire_audience_hygiene_export_status` | Private artifact/read | Begin creates a private checkpoint job under an approved output root, stores state in a deterministic `checkpoint-<sha256(job_id)>` child, returns aggregate/redacted progress only, and keeps `artifact_prefix` limited to export artifact names plus explicit legacy checkpoint recovery on resume/status. Resume/status must resolve only the deterministic child or one validated legacy `<artifact_prefix>-<job_id>` direct child, normalize loaded state to the resolved directory, and update the schema snapshot when request fields change. | Disallowed root, output symlink, state/temp symlink, malformed or wrong `job_id`, mismatched stored job id, missing default-prefix legacy checkpoint, wrong explicit legacy prefix, sibling directory scan regression, raw recipient leakage, over-budget query count. | Run begin, status, and resume against a small target list on the target Interspire major version with a private approved output root; confirm no send, schedule, import, queue, or stats change, and separately inspect the private artifact only outside public output. |
| Queue/stat posture | `interspire_queue_stats_readback` | Read | Schedule and Stats rows redacted and capped. | Raw recipient leakage, capped warning, parse fallback. | Read before and after every no-send proof/apply operation. |
| Send wizard proof | `interspire_send_wizard_readback` / `interspire_seed_readiness_gate` | No-send proof | Renders only the final editable pre-send state, returns campaign/list/count/sender/tracking proof, and proves queue/stats unchanged. | Wrong list/campaign, count mismatch, send boundary attempted, queue/stat changed. | Render target draft/list through no-send proof only; no send, no schedule, no cron. |
| Guarded send apply | `interspire_seed_send_apply` / `interspire_production_send_apply` | Guarded send apply | Re-runs readiness immediately, then posts only the allowlisted final send boundary with campaign id and list ids bound from the proven request rather than trusting de-selected or omitted Step4 controls. `sent=true` requires terminal queue/stats proof plus an Interspire job id. | Interspire 8 Step4 echoes `newsletter=0`, omits selected lists, returns HTTP 200 without a popup/job URL, exposes stale list controls, changes queue/stats during no-send proof, missing send gates, wrong subject/hash/count/sender. | Use a synthetic fixture for de-selected campaign and omitted/stale list controls. On the target major version, run the tool only against an approved seed after no-send proof; HTTP 200 without job plus queue/stats movement must return `posted-unproven`/non-success. |
| OCI send ledger preparation | `interspire_oci_send_ledger_prepare_preview` / `interspire_oci_send_ledger_prepare_apply` | Private artifact / guarded local apply | Preview reads a private direct-child JSONL manifest and returns a deterministic plan without writing. Apply requires guarded writes, send controls, exact plan id, and `acknowledge_ledger_write=true`, appends sanitized private ledger rows with an apply-time UTC `submitted_at`, then reruns OCI ledger preflight. Existing exact rows are idempotent only while their `submitted_at` is fresh enough for the immediate send boundary. It never contacts OCI or performs an Interspire send, schedule, queue, import, contact, list, or suppression mutation. | Missing env path, manifest outside the configured ledger directory, group/world-readable manifest or ledger directory, symlink/non-file manifest or ledger, stale plan id, missing acknowledgement, partial existing matching rows, stale exact matching rows, wrong row count, missing recipient key, missing message/correlation/header key, malformed `*_hash` field, timestampless appended row, duplicate keys, raw recipient/trace leakage. | Use a synthetic private manifest and ledger path; prove preview writes nothing, apply writes only timestamped hashes/counts, post-write preflight verifies, stale plans do not write, stale exact rows cause fresh appended rows rather than accepted idempotence, and no Interspire admin send automation is reached. |
| OCI send ledger preflight | `interspire_seed_send_apply` / `interspire_production_send_apply` `oci_ledger_preflight` | Send-adjacent gate | When `INTERSPIRE_REQUIRE_OCI_SEND_LEDGER=1`, the guarded send refuses before the final send form unless the configured private JSONL ledger contains the expected Interspire campaign/batch rows with recipient keys, trace keys, and valid UTC `submitted_at`/timestamp values that are no older than 15 minutes and no more than 5 minutes in the future. Output returns hashes and counts only, including `stale_rows_ignored` when otherwise matching rows were too old, too far in the future, invalid, or missing timestamps. Ledger rows may be produced by the generic prepare tools or by an operator-owned private process that writes the same contract. | Missing env path, missing preflight request, unsafe configured ledger path or permissions, ledger campaign id not equal to the Interspire campaign being sent, wrong row count, missing recipient key, missing message/correlation key, missing/invalid/stale/future timestamp, invalid JSON row, raw campaign/recipient leakage, per-call path attempt. | Use a synthetic private ledger file and gated send request on a non-sending fixture path; prove the report blocks before admin send automation when ledger rows are absent, tied to another campaign, missing timestamps, or stale, and verifies only hashes/counts when fresh rows are present. |

Rows may be deferred only when the deferred state is recorded in Ops and the
operator workflow does not depend on that capability.

## Failure Handling

If any live proof fails:

1. Stop operational use of that capability.
2. File or update an Ops friction report with the exact redacted failure.
3. Add a fixture or regression test for the discovered shape.
4. Patch the MCP and rerun the affected row plus adjacent rows that depend on
   it.
5. Rebuild through hosted GitHub Actions, install with checksum verification,
   restart Codex, and repeat the live no-send proof before handoff.
