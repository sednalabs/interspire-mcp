# Live Proof Matrix

This repository wraps newsletter control planes. A tool is not operationally
ready just because it compiles, appears in the MCP tool list, or was installed
from a hosted binary. For every new or materially changed capability, update
this matrix before coding and record the exact proof before handoff.

## Acceptance Rule

Each operational capability needs all of the following:

- a named workflow row below;
- fixture or unit coverage for the parser, route gate, redaction, and output
  contract;
- negative coverage for the most likely unsafe or misleading state;
- public docs and schema snapshot updates when the tool contract changes;
- hosted GitHub Actions validation for release candidates;
- hosted binary artifact plus checksum verification for installable binaries;
- configured-alias startup proof after install;
- live no-send proof on the target Interspire major version before operational
  use.

Tool listing, successful initialization, and checksum install are installation
proof only. They do not prove that the workflow is usable.

Do not discover this matrix one failure at a time. Before changing a capability,
identify every affected row, update the expected contract, then run the complete
focused gate for those rows before release or install.

## Interspire Draft Prep Matrix

| Workflow | Tool | Class | Required Proof | Negative Tests | Live No-Send Proof |
| --- | --- | --- | --- | --- | --- |
| Runtime posture | `interspire_status` | Read | Reports admin/XML configuration, gates, blocked operations, version target, and a capability list that matches the router tool names. | Missing config, disabled risky gates, stale alias, router/status capability drift. | Status called after install/restart on the target alias and compared with tool discovery for the same alias. |
| Admin session | `interspire_admin_session_probe` | Read | Login succeeds and allowlisted read page loads; Send start page optional. The live backend must reuse one serialized admin client per MCP process so adjacent tools do not churn Interspire logins or race a shared session. | Bad CSRF/session, Access missing, blocked route, repeated login churn, accidental parallel admin proof calls. | Probe target admin with `include_send_start=true`; no queue/stat change. Immediately run one adjacent admin readback through the same configured alias to prove the session remains usable. |
| Settings posture | `interspire_settings_audit` | Read | Redacted SMTP, bounce, throttle, unsubscribe, and cron fields. | Secret redaction, missing tab, cron omitted when requested. | Audit target settings; no form submit. |
| Settings inventory | `interspire_settings_inventory` | Read | Fetches only allowlisted settings tabs, returns capped redacted form fields, and reports omitted secret-shaped, hidden, blank, or unchecked controls by name and reason. | Password/token/license/key/cookie fields omitted, hidden control values summarized when explicitly requested, fields plus omissions capped together, missing tab, no form submit. | Inventory target settings with `include_cron=true`; confirm no form submit and no queue/stat change. |
| Settings write | `interspire_settings_update_preview` / `interspire_settings_update_apply` | Preview/apply | Preview captures the exact settings form and apply persists only allowlisted non-secret fields after guarded write gates are enabled. Checkbox controls must report and prove checked state, not the static HTML value attribute. Settings tab hidden state such as `tab_num` must be preserved so tab-scoped saves are browser-equivalent. Cron writes use `section="cron"` and only `cron_enabled`, `cron_send`, `cron_bounce`, `cron_autoresponder`, `cron_triggeremails_s`, and `cron_maintenance`; they do not trigger `cron.php`. | Wrong section, unsupported field, stale plan id, missing write gates, secret/password field, CSRF/session expiry, checkbox value mistaken for checked state, hidden settings tab state omitted, saved-banner without field readback, accidental cron trigger route. | On the target major version, preview/apply one harmless cron setting change on an approved maintenance window, then re-read settings and Schedule/Stats to confirm the field persisted and no cron/send action occurred. |
| List ownership | `interspire_list_owner_readback` | Read | Existing list sender/reply/bounce metadata, redacted. | Capped output, HTML enrichment failure, email redaction. | Read target lists before and after list create/update. |
| Exact contact state | `interspire_contact_state` | Read | Checks one exact email/list pair through XML `IsSubscriberOnList`, falls back to bounded XML `GetSubscribers` exact-email search when the direct probe is false, then uses exact admin-HTML corroboration when configured. Presence requires an exact active, confirmed, non-bounced, non-unsubscribed subscriber record; uncorroborated absence remains low confidence. | Wildcard or malformed email rejected before XML exact search, false direct XML probe plus exact-search positive returns presence with a method-disagreement warning, raw email redaction, no send/import/queue/stat mutation. | Read one approved seed address on the target list after install/restart; if XML/admin disagree, record the disagreement and do not use negative contact-state alone as a send-readiness blocker. |
| List creation | `interspire_list_create_preview` / `interspire_list_create_apply` | Preview/apply | Preview finds the Interspire create form and source-derived `AddList` route; apply submits browser-equivalent hidden fields, selected controls, save control when named, Referer, Origin, and current page CSRF context, creates exactly one new list, then proves requested metadata on the new edit page, using a guarded post-create metadata save if Interspire ignored Bounce Email on create. If the default Lists inventory does not expose the created id after POST, apply may use an allowlisted `Lists&Action=Edit&id=...` redirect id, but only after confirming it was not in the before inventory and still proving the edit page metadata. | Missing form, stale plan, missing gates, wrong action route, missing/empty form CSRF with page-level CSRF present, duplicate new ids, conflicting inventory/redirect id proof, redirect to existing id, field persistence mismatch, webhook/multi-select state loss. | Preview and apply on a disposable target list; reread list owner metadata and confirm local bounce polling was not enabled unless explicitly requested. |
| XML scaffold candidates | deferred XML list/newsletter scaffold tools | Preview/apply | Deployment-specific compatibility evidence may identify XML list/newsletter scaffold candidates, but no XML write tool is operational until request details, permission behavior, response shape, and post-write readback are fixture-proven. | Missing XML auth, method not allowlisted, permission denial, malformed details, ambiguous success, object not created/copied, redaction leak. | Deferred. If implemented, run on a disposable target object only, then prove list/campaign readback and queue/stats unchanged. |
| Campaign inventory | `interspire_campaign_readback` | Read | Returns redacted campaign manage rows plus structured campaign ids/action flags without admin URLs or CSRF tokens. | Linked campaign-name rows, token leakage, email leakage, capped output. | Read target campaign inventory and confirm the intended source campaign id before copy/edit. |
| Campaign copy | `interspire_campaign_copy_preview` / `interspire_campaign_copy_apply` | Preview/apply | Preview finds the exact Copy route or safely constructs the same Copy route for an off-page source campaign from another allowlisted Copy link on the manager page; apply creates exactly one new draft id. | Wrong campaign id, stale plan, duplicate id detection, route with extra params, constructed route that fails the exact campaign-copy allowlist. | Copy a known disposable/source campaign, including at least one source id that is not visible on the first campaign-manager page; reread new campaign row. |
| Campaign active state | `interspire_campaign_active_state_preview` / `interspire_campaign_active_state_apply` | Preview/apply | Preview reads the campaign manager page, infers current active state only from exactly one visible Activate or Deactivate affordance for the exact campaign id, and binds the requested active state into the plan id without volatile CSRF/session values. Apply requires form-write gates, executes only the allowlisted `Newsletters&Action=Activate|Deactivate&id=...` route for that campaign, then proves the requested active state from a fresh campaign-manager readback. HTTP success without matching fresh readback is a failure. | Missing campaign row, both Activate and Deactivate exposed, neither state action exposed, wrong campaign id, unsupported action, extra query parameters, stale plan id, disabled write gates, route returns login page, route HTTP success without state readback. | Deactivate and reactivate a disposable draft on the target major version, prove the manage row flips, and confirm no send, queue, import, contact, list, provider, DNS, or SMTP mutation occurred. |
| Campaign metadata update | `interspire_campaign_update_preview` / `interspire_campaign_update_apply` | Preview/apply | Preview captures campaign metadata controls on the form where they actually live. Interspire 8 Step1-only fields such as `name` must be applied separately from body/Step2 fields: apply posts the Step1 wizard handoff, posts the preserved Complete/save form, then proves the Step1 field from a fresh admin session. Campaign names are compared with Interspire-safe punctuation normalization because the live app can strip punctuation during Step1 save; missing or reordered words still fail proof. Step2/final-form fields such as subject, tracking flags, body controls, the documented Archive checkbox, and recipient-facing `PreHeader` stay on the existing final-save path and reread the campaign form/body audit as applicable. | Missing Step1 controls, mixed Step1-only and body fields, unsupported field, stale plan, hidden field loss, archive checkbox value confused with checked state, preheader omitted from inbox-preview proof, tracking flag drift, Step1 handoff without durable Complete/save proof, name normalization that drops words rather than punctuation. | Apply a harmless Step1-only name change to a disposable draft, prove it from a fresh session, then separately apply a harmless final-form metadata change including `PreHeader`; queue unchanged and no new Stats rows. |
| Campaign template edit | `interspire_campaign_template_update_preview` / `interspire_campaign_template_update_apply` | Preview/apply | Preview resolves body controls; apply persists subject/body/tracking fields and rereads them. Text-body edits against HTML-only Interspire 8 campaigns must first render Step2 with the Text+HTML format selected, then prove non-zero text bytes on body audit after apply. Step1-only campaign name changes belong in the campaign metadata row and are deliberately kept separate from body edits. | Missing Step2 controls, stale plan, hidden field loss, tracking flag drift, HTML-only campaign lacking `TextContent` until Step1 format is changed, attempted mixed Step1 name plus body edit. | Apply a harmless draft-only HTML change to a disposable draft; apply a harmless text-body change to an HTML-only disposable draft and audit `text_bytes > 0`; queue/stats unchanged. |
| Campaign template artifact transfer | `interspire_campaign_template_artifact_update_preview` / `interspire_campaign_template_artifact_update_apply` | Preview/apply | Preview reads only a fixed private render artifact, verifies optional bytes/SHA-256, returns no raw body, and delegates to guarded template preview; apply repeats the artifact read, saves the draft, then fails unless post-apply body audit matches the artifact hash. | Path traversal, symlink artifact, oversized artifact, bad expected digest, stale plan, raw HTML leakage, persisted hash mismatch, send boundary attempted. | Apply a private source artifact to a disposable/new draft; audit body and queue/stats unchanged. |
| Campaign body audit | `interspire_campaign_body_audit` | Read/no-send proof | Counts unsubscribe tokens, URL protocols, images, alt text, links, preheader SHA-256, and body hashes without raw HTML or raw preheader text. | Missing body form, raw HTML leakage, raw preheader leakage, secret leakage. | Audit target draft and record warnings. |
| Render artifact | `interspire_campaign_render_artifact` | Private artifact | Writes private preview files under approved root and returns paths/hashes only. | Path traversal, disallowed root, raw HTML in response. | Render target draft to private artifact root; open separately for visual proof if needed. |
| One-recipient campaign preview send | `interspire_campaign_test_send_preview` / `interspire_campaign_test_send_apply` | No-send proof / guarded test send | Preview reads persisted campaign subject/body hashes, binds one exact recipient plus caller-supplied preview sender, verifies queue/stat invariants, and returns a preview digest without posting. Apply requires `INTERSPIRE_GUARDED_WRITES=1`, `INTERSPIRE_SEND_CONTROLS=1`, `acknowledge_test_send=true`, one exact recipient, exact preview digest, the preview report's public subject, and exact preview HTML SHA-256, then posts only Interspire's native `Newsletters/SendPreview` route. Output stays redacted and says this path does not prove list-specific unsubscribe, merge fields, tracking, or production audience behavior. Denied apply reports must omit campaign-body proof entirely rather than returning synthetic fixture data. | Multiple recipients, missing acknowledgement with no campaign_body returned, disabled send gate, wrong expected preview digest/subject/hash, empty campaign body, extra query parameter on SendPreview route, raw email/body leakage, queue/stat row changes, response that is not an explicit successful preview send. | On the target major version, run preview against a draft and apply only to an approved seed mailbox; verify Schedule and Stats rows are unchanged before/after. Use seed-list send proof instead when unsubscribe/list metadata behavior must be proven. |
| CSV import preflight | `interspire_contact_import_preflight` | Read/local file proof | Reads only configured private roots; returns headers, aggregate counts, duplicates, invalid-looking count, and hash. | Disallowed root, `..`, non-CSV, raw row/email leakage. | Preflight a synthetic private CSV before any real audience artifact. |
| Audience hygiene checkpoint export | `interspire_audience_hygiene_export_begin` / `interspire_audience_hygiene_export_resume` / `interspire_audience_hygiene_export_status` | Private artifact/read | Begin creates a private checkpoint job under an approved output root, stores state in a deterministic `checkpoint-<sha256(job_id)>` child, returns aggregate/redacted progress only, and keeps `artifact_prefix` limited to export artifact names plus explicit legacy checkpoint recovery on resume/status. Resume/status must resolve only the deterministic child or one validated legacy `<artifact_prefix>-<job_id>` direct child, normalize loaded state to the resolved directory, and update the schema snapshot when request fields change. | Disallowed root, output symlink, state/temp symlink, malformed or wrong `job_id`, mismatched stored job id, missing default-prefix legacy checkpoint, wrong explicit legacy prefix, sibling directory scan regression, raw recipient leakage, over-budget query count. | Run begin, status, and resume against a small target list on the target Interspire major version with a private approved output root; confirm no send, schedule, import, queue, or stats change, and separately inspect the private artifact only outside public output. |
| Queue/stat posture | `interspire_queue_stats_readback` | Read | Schedule and Stats rows redacted and capped. The read must use the same serialized admin client/session discipline as send-wizard proof so a standalone monitor cannot fail immediately after a successful no-send gate. | Raw recipient leakage, capped warning, parse fallback, login-page response after a prior admin proof, stale session overclaimed as readiness. | Read before and after every no-send proof/apply operation. After a restart, run admin session probe, queue/stat readback, no-send wizard proof, and queue/stat readback again sequentially on the target alias. |
| Queue controls | `interspire_queue_control_preview` / `interspire_queue_control_apply` | Preview/apply | Preview reads the Schedule and newsletter Manage pages, returns redacted candidate rows with an explicit source, and builds plan ids from the canonical non-secret route, requested action, source, exact numeric row/job identity, method, and campaign association when available. Schedule candidates retain cancel/pause/resume and one-job delete-form handling. Manage-only immediate jobs may expose only the exact `Send` pause/resume/delete actions for one positive numeric `Job`; they must never be inferred from campaign names, recipient counts, Stats rows, or broad page text, and the route source must match the authenticated page that exposed it. Plan ids intentionally exclude volatile row text, query order, and session/CSRF values. Preview/status/apply fail closed when either source reaches the row cap or exposes pagination/result-limit controls. Apply requires `INTERSPIRE_GUARDED_WRITES=1`, `INTERSPIRE_QUEUE_WRITE_CONTROLS=1`, `acknowledge_queue_mutation=true`, the exact plan id, and exact action. It re-reads both sources before applying the allowlisted route with the source-correct referer, admits only authenticated same-admin redirects to Schedule or Manage, then re-reads both sources. Cancel/delete are proven only when complete bounded inventories no longer expose the target on either source; pause/resume are proven only when the requested action is gone and the expected opposite action appears for the same job on an authoritative source. | Unsupported action or source, send/schedule/cron/import/contact routes, duplicate/smuggled or malformed identifier aliases, duplicate plan identities, stale or action/source-mismatched plan id, missing acknowledgement, hidden route/token leakage, wrong delete checkbox, mismatched pause preflight, authentication loss after login or apply, login/external/unallowlisted redirect or HTTP denial, pagination/result-limit/capped disappearance proof, target still exposing the requested action, pause/resume without opposite action, cross-source contradiction, raw recipient leakage, active progress text changing between preview and apply. | On the target major version, preview a disposable Schedule row and a Manage-only zero-send immediate row. Apply pause to each approved disposable target, prove the source transition plus cross-source queue readback, and confirm no send, cron, import, contact, list, provider, DNS, or SMTP mutation occurred. Fixture proof must cover changing progress text, Manage-only `In Progress`, duplicate historical recipient counts, stale preview, response loss, replay, authenticated same-admin redirect, login/external redirect, cross-page links, malformed aliases, duplicate plans, pagination/result limits, and capped disappearance readback. Run cancel/delete live proof only on an operator-approved disposable zero-send target. |
| Structured send-job status | `interspire_send_job_status_readback` | Read | Reads Schedule, newsletter Manage, and Stats. It binds one expected job id and carries caller-bound campaign/list/expected-total context. Active identity requires an exact allowlisted queue-control route for that numeric job from Schedule or Manage; Manage row association is returned only when the action is isolated to one campaign row, and conflicting sources fail closed. Stats are aggregate historical context only and never establish job identity from recipient count, campaign-id-like text, dates, or a unique-looking row after the queue action disappears. The report exposes redacted rows/action plans and reports direct queue/unsent counters unavailable unless a reviewed source exists. | Wrong job id, expected total mismatch, stale row without a matching action route, Manage action not isolated to one campaign row, source contradiction, zero Schedule matches, job-id/campaign-id token collision, capped Stats slice, one or many Stats rows with the same expected total, exact list scope overclaim, raw recipient leakage, fabricated queue-table processed counts, fabricated unsent reason aggregates. | On the target major version, read one Schedule job and one Manage-only immediate job. Confirm identity/follow-up contracts require an exact current action route and that zero Schedule/Manage matches remain unverified even when one or more historical Stats rows share the expected total. No queue control apply, send, cron, import, contact, list, provider, DNS, or SMTP mutation occurs. |
| Cron readiness | `interspire_cron_readiness` | Read | Reads cron settings plus Schedule-page cron detection text without triggering `cron.php`; production readiness is false unless the master `cron_enabled` checkbox is proven enabled and runner proof is present. Positive interval fields such as `cron_send=5` are not enough because Interspire's runtime gate is `SENDSTUDIO_CRON_ENABLED`; stale `Last Run` text alone is not runner proof. | App cron enabled but server runner missing, stale/not-detected cron warning, master `cron_enabled` omitted/unchecked while interval fields are non-zero, omitted cron settings, accidental cron trigger route, raw host/path leakage. | On the target major version, run against Settings/Schedule and compare with independently known cron-runner state before production sends; no cron route is requested. |
| Send stop-gate readiness | `interspire_send_stop_gate_readiness` | No-mutation proof | Composes structured send-job status and optional OCI ledger preflight into a hold/continue/pause recommendation. If pause is recommended, output carries the exact pause plan id from the Schedule preview surface and still requires separate queue-control apply. | OCI requested but unverified, threshold reached without pause action, active job not proven, expected total mismatch, recommendation overclaims zero bounces when provider data is unavailable, raw recipient leakage. | On the target major version, run during a small approved send window and confirm any pause recommendation is read-only until an operator separately calls queue-control apply with the returned plan. |
| Send wizard proof | `interspire_send_wizard_readback` / `interspire_seed_readiness_gate` | No-send proof | Renders only the final editable pre-send state, returns campaign/list/count/sender/tracking proof, and proves the Schedule queue unchanged plus no new or removed stable Stats rows. Existing Stats-row campaign-name, list-label encoding, unsubscribe, and bounce counter text can change while a prior campaign continues receiving engagement; that remains a warning only when the existing row's started timestamp, finished timestamp, and recipient count stay stable. Queue growth, added Stats rows, removed Stats rows, or shifted row identities remain blockers. | Wrong list/campaign, count mismatch, send boundary attempted, queue changed, new/removed Stats row, row identity mismatch, raw recipient leakage. | Render target draft/list through no-send proof only; no send, no schedule, no cron. Include fixtures where an existing Stats row campaign name, list-label encoding, or mutable counters change without a new row, plus one where a new Stats row blocks. |
| Guarded send apply | `interspire_seed_send_apply` / `interspire_production_send_apply` | Guarded send apply | Re-runs readiness immediately, then posts only the allowlisted final send boundary with campaign id and list ids bound from the proven request rather than trusting de-selected or omitted Step4 controls. Cron-enabled Step4 confirmations must follow only the exact session-bound Schedule approval continuation and extract the resulting job id from Schedule readback. `sent=true` requires terminal queue/stats proof plus an Interspire job id. Queued-not-proven jobs return a follow-up contract for `interspire_send_job_status_readback`. Early refused applies that happen before readiness proof must omit campaign-body and send-wizard proof blocks rather than returning synthetic fixture data. Readiness warnings from no-send proof are preserved in the apply receipt, but only failed blocker gates, expected subject/hash mismatches, unsafe final-form proof, ledger failure, or missing runtime gates can refuse the send before the final boundary. Failed post-send reconciliation is reported as a post-boundary non-success state. | Interspire 8 Step4 echoes `newsletter=0`, omits selected lists, returns HTTP 200 without a popup/job URL, exposes stale list controls, changes queue/stats during no-send proof, missing send gates, wrong subject/hash/count/sender, unsafe or parameter-smuggled Schedule approval route, cron approval followed but no durable job id, queued job without follow-up contract, refused apply containing fixture campaign/send-wizard proof, non-blocking no-send warnings treated as fatal refusal reasons. | Use synthetic fixtures for de-selected campaign and omitted/stale list controls, plus a cron confirmation page whose approval button targets `index.php?Page=Schedule&A=1`. On the target major version, run the tool only against an approved seed after no-send proof; HTTP 200 without job plus queue/stats movement must return `posted-unproven`/non-success, queued jobs must be refreshable through the status readback contract, and harmless no-send stats-content churn must be returned as a warning without blocking the apply. |
| OCI send ledger preparation | `interspire_oci_send_ledger_prepare_preview` / `interspire_oci_send_ledger_prepare_apply` | Private artifact / guarded local apply | Preview reads a private direct-child JSONL manifest and returns a deterministic plan without writing. Apply requires guarded writes, send controls, exact plan id, and `acknowledge_ledger_write=true`, appends sanitized private ledger rows with an apply-time UTC `submitted_at`, then reruns OCI ledger preflight. Existing exact rows are idempotent only while their `submitted_at` is fresh enough for the immediate send boundary. Reports classify message-id, header-value, and local correlation-only trace rows; provider-visible candidate counts are evidence for planning, not proof that provider logs exposed the join key. It never contacts OCI or performs an Interspire send, schedule, queue, import, contact, list, or suppression mutation. | Missing env path, manifest outside the configured ledger directory, group/world-readable manifest or ledger directory, symlink/non-file manifest or ledger, stale plan id, missing acknowledgement, partial existing matching rows, stale exact matching rows, wrong row count, missing recipient key, missing message/correlation/header key, malformed `*_hash` field, timestampless appended row, duplicate keys, raw recipient/trace leakage, overclaiming local correlation-only rows as exact provider traceability. | Use a synthetic private manifest and ledger path; prove preview writes nothing, apply writes only timestamped hashes/counts and trace-classification counts, post-write preflight verifies, stale plans do not write, stale exact rows cause fresh appended rows rather than accepted idempotence, local correlation-only rows remain distinguishable from provider-visible candidates, and no Interspire admin send automation is reached. |
| OCI send ledger preflight | `interspire_seed_send_apply` / `interspire_production_send_apply` `oci_ledger_preflight` | Send-adjacent gate | When `INTERSPIRE_REQUIRE_OCI_SEND_LEDGER=1`, the guarded send refuses before the final send form unless the configured private JSONL ledger contains the expected Interspire campaign/batch rows with recipient keys, trace keys, and valid UTC `submitted_at`/timestamp values that are no older than 15 minutes and no more than 5 minutes in the future. Output returns hashes and counts only, including `stale_rows_ignored` when otherwise matching rows were too old, too far in the future, invalid, or missing timestamps. Ledger rows may be produced by the generic prepare tools or by an operator-owned private process that writes the same contract. | Missing env path, missing preflight request, unsafe configured ledger path or permissions, ledger campaign id not equal to the Interspire campaign being sent, wrong row count, missing recipient key, missing message/correlation key, missing/invalid/stale/future timestamp, invalid JSON row, raw campaign/recipient leakage, per-call path attempt. | Use a synthetic private ledger file and gated send request on a non-sending fixture path; prove the report blocks before admin send automation when ledger rows are absent, tied to another campaign, missing timestamps, or stale, and verifies only hashes/counts when fresh rows are present. |

Rows may be deferred only when the deferred state is recorded in Ops and the
operator workflow does not depend on that capability.

## Failure Handling

If any live proof fails:

1. Stop operational use of that capability.
2. File or update an Ops friction report with the exact redacted failure.
3. Add a fixture or regression test for the discovered shape.
4. Patch the MCP and rerun the affected row plus adjacent rows that depend on
   it.
5. Rebuild through hosted GitHub Actions, install with checksum verification,
   restart Codex, and repeat the live no-send proof before handoff.
